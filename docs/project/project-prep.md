# The Last Host 프로젝트 준비안

최종 수정일: 2026-06-30

## 목적

이 문서는 `마지막 숙주 / The Last Host`의 구현을 시작하기 전에 현재 준비 상태를 정리하기 위한 문서다. 확정된 방향, 승인 상태, 저장소 상태, Unity 프로젝트로 전환하기 전 필요한 순서를 정리한다.

## 현재 저장소 상태

- 작업 위치: `C:\project\Last-Host`
- Git 원격 저장소: `https://github.com/LEEBEOMSHIK/Last-Host.git`
- Unity 프로젝트 루트: `UnityProject/`
- Unity 버전: `6000.4.6f1`
- 렌더 파이프라인: URP 패키지 포함
- 현재 단계는 Unity 프로젝트 골격을 루트 저장소에 편입하고 프로토타입 구현 전 준비를 정리하는 단계다.

## 원본 기획 자료

- 원본 초안: `C:\project\game\last_host\last_host_game_plan.docx`
- 초안의 핵심 방향은 도트풍 저폴리 3D 진화 생존 로그라이크다.
- 초안은 핵심 루프 검증을 위해 `쥐 숙주 프로토타입`부터 시작하는 방향을 제안한다.

## 확정된 결정

- 엔진: Unity 3D
- 비주얼: 순수 2D 도트가 아닌 도트풍 3D
- 렌더링 방향:
  - 저폴리 3D 모델
  - 픽셀풍 텍스처
  - 저해상도 렌더링 또는 후처리
- 게임 구조:
  - 바깥 세계의 숙주 조종 탐험
  - 숙주 내부의 바이러스 면역 미니게임
  - 변이 선택과 성장
- 첫 프로토타입 초점: 하수도 환경의 쥐 숙주
- 쥐는 벌레보다 상위 숙주지만, 첫 프로토타입에서는 정식 진행 순서가 아니라 핵심 루프 검증용 수직 슬라이스로 사용한다.
- 1차 프로토타입에는 벌레 튜토리얼을 포함하지 않는다.

## 구현 승인 상태

2026-06-30 기준, 사용자가 쥐 숙주 1차 프로토타입 구현에 필요한 항목을 전체 승인했다.

1. Unity 버전
2. URP 사용 여부
3. PC 우선 프로토타입 확정 여부
4. 첫 프로토타입의 성공과 실패 조건
5. 초기 조작 방식
6. 초기 UI 게이지 구성
7. 플레이스홀더 에셋 사용 범위
8. 프로토타입 변이 효과

승인 이력은 `docs/prototype/approvals/rat-host-approval-packet.md`에 정리한다.

참고: 현재 Unity 프로젝트는 `UnityProject/`에 편입되어 있으며, 1차 프로토타입은 Unity `6000.4.6f1`과 URP를 기준으로 진행한다.

## 권장 문서 구성

- `docs/design/game-design-summary.md`: 원본 `.docx` 기반 게임 기획 요약
- `docs/prototype/official/rat-host-prototype.md`: 첫 플레이어블 프로토타입 범위
- `docs/prototype/plans/rat-host-implementation-plan.md`: 승인된 쥐 숙주 프로토타입 구현 계획
- `docs/project/project-prep.md`: 현재 프로젝트 준비 상태와 승인 계획
- `AGENTS.md`: Codex와 이후 작업자가 따라야 할 프로젝트 규칙

## 권장 마일스톤

### 마일스톤 0: 준비

- 기획서 초안을 작업용 Markdown 문서로 정리한다.
- 엔진, 비주얼 방향, 프로토타입 범위, 승인 게이트를 확정한다.
- Unity 버전과 렌더 파이프라인을 결정한다.

### 마일스톤 1: Unity 프로젝트 골격

- Unity 3D 프로젝트 골격을 `UnityProject/`에서 관리한다.
- 저장소 정리, `.gitignore`, 기본 폴더, 렌더 파이프라인 설정을 확인한다.
- 플레이스홀더 카메라와 조명이 있는 최소 테스트 씬을 만든다.

### 마일스톤 2: 쥐 숙주 핵심 루프

- 작은 하수도 맵에서 쥐를 움직인다.
- 면역 경계도 게이지를 적용한다.
- 내부 바이러스 미니게임으로 전환한다.
- 바이러스 이동, 백혈구 적, 변이 조각 수집을 구현한다.
- 변이 선택 후 쥐 숙주 모드로 복귀한다.

### 마일스톤 3: 프로토타입 감각 조정

- 도트풍 3D 렌더링을 조정한다.
- 카메라를 다듬는다.
- 기본 UI 가독성을 확인한다.
- 플레이스홀더 애니메이션과 피드백을 추가한다.
- 짧은 플레이테스트 체크리스트로 핵심 루프를 검증한다.

## 바로 다음 단계

1. 승인된 구현 계획을 기준으로 Unity 프로젝트 변경 작업 패킷을 만든다.
2. `RatHostPrototype.unity`, `Assets/_Project/` 구조, 입력 액션 자산을 준비한다.
3. 쥐 숙주 핵심 루프 구현을 작은 커밋 단위로 진행한다.
4. 구현 후 컴파일, 테스트, 수동 플레이, PC 빌드 검증을 실행한다.
