# 검증 기록

## 작업 ID

`2026-07-24-rat-final-appearance-sample`

## 검증 대상

쥐 최종 외형 근접 샘플의 원본·규격·8방향·포즈·픽셀 처리·사용자 제시 가능 여부.

## 2026-07-24 독립 QA

### 판정

- **수정 필요**
- **현재 결과는 사용자 제시 불가**
- 차단 사유: E 정지·보행 키의 꼬리 실루엣이 우측 캔버스 경계에서 잘린다.

### 자동 대조 결과

- 원본·재현 자료
  - `artifacts/source/rat-final-appearance-sample-v1.blend`: 존재, 127,953 bytes
  - `artifacts/source/create_rat_final_appearance_sample.py`: 존재, Python AST 구문 분석 통과
- 방향 PNG
  - 정지 8개 + 보행 키 8개 = 16개
  - 파일명·방향 순서: `S, SW, W, NW, N, NE, E, SE`
  - 전부 `128×128`, `RGBA`
  - 전체 알파 값: `[0, 255]`
  - 16장 공용 불투명색: 25색, 최대 32색 계약 충족
  - 정지/보행 키 동일 방향 픽셀 차이: 방향별 302~515픽셀로 모든 방향에서 실제 포즈 차이 확인
- 부가 산출물
  - 비교표: `2048×512`, 32-bit RGBA
  - 턴어라운드 프리뷰: `2048×640`, 32-bit RGBA
  - `frame-map.csv`, `render-settings.json`, `palette-statistics.json`의 장수·방향·프레임·팔레트 기록은 실제 PNG와 일치
- 픽셀 처리
  - 반투명 가장자리 없음
  - 공용 팔레트 25색
  - 팔레트 최근접 치환 방식이며 디더링 처리 없음

### 시각 대조 결과

- 도시 하수도 갈색쥐, 낮은 무게중심, 따뜻한 회갈색 등·탁한 베이지 배·저채도 분홍갈색 귀/발/꼬리 방향은 브리프와 대체로 일치한다.
- 머리·몸통, 큰 귀, 뾰족하지만 과도하게 길지 않은 주둥이, 작은 눈·코, 앞/뒤 발의 구분은 8방향 비교표에서 판독 가능하다.
- 보행 키는 정지보다 발 배치와 몸통/머리 리듬이 달라지고 큰 상하 점프가 없어 대표 잰걸음 키로 판독 가능하다.
- 방향별 형태 투영 차이는 있으나 동일 직교 카메라·조명·공통 원점 기준은 설정 기록과 일치한다.

### 차단 결함

- `artifacts/renders/idle/rat-final-sample-idle-06-e.png`
  - 알파 경계 `x=127`에 불투명 픽셀 3개가 남는다.
  - 꼬리 선이 마지막 열까지 두께를 유지한 채 이어져 자연 종료점이 아니라 캔버스 밖으로 계속되는 절단 형태다.
- `artifacts/renders/walk-key/rat-final-sample-walk-f04-06-e.png`
  - 알파 경계 `x=127`에 불투명 픽셀 2개가 남는다.
  - 정지와 동일하게 꼬리 끝이 캔버스 밖에서 잘린다.
- W 정지·보행 키는 좌측 최소 `x=1`로 경계 접촉은 없지만 여백이 1픽셀뿐이다. E 수정 시 W도 다시 경계 대조한다.
- 이는 브리프의 꼬리 실루엣 판독 질문과 방향 전환 시 점유율/실루엣 안정성 질문을 통과하지 못한다. 브리프의 “핵심 실루엣을 방해하면 사용자 제시 전 수정” 조건에 따라 차단 결함으로 판정한다.

### 제작 담당 재작업 요청

1. 모든 방향에 투명 경계가 남도록 카메라 직교 크기 또는 프레이밍을 조정한다. E 우측은 최소 1개 투명 열을 확보하고 W 좌측 1픽셀 여백도 함께 재확인한다.
2. 같은 원본·카메라·팔레트 계약으로 정지 8장과 보행 키 8장, 비교표, 턴어라운드, 설정·통계 파일, `.blend`를 다시 생성한다.
3. 재생성 뒤 16장 전체 경계 접촉 0건, `128×128 RGBA`, 이진 알파, 공용 32색 이하를 다시 독립 QA한다.

### 재현성과 보존 범위

- 재현 스크립트는 현재 활성 작업 절대 경로를 사용한다. 작업 폴더 보관 후에는 산출물을 보관 폴더 옆이 아니라 기존 `_workspace/active/...`에 다시 만들 수 있으므로, 재작업 시 스크립트 위치 기준 경로로 바꾸거나 보관 후 재현 절차를 명시하는 것이 안전하다.
- 현재 작업 산출물은 `_workspace/completed/2026-07-27-2026-07-24-rat-final-appearance-sample/` 아래에 보관되어 있다.
- `UnityProject/`, `Builds/`, `_workspace/completed/`는 존재하며 이 QA에서 변경하지 않았다. UnityProject의 기존 수정 2건은 유지되어 있고 삭제 상태는 없다.
- 범위에 따라 Unity 실행·반입·Import 설정·씬·ProjectSettings·Builds 검증은 수행하지 않았다.

## 2026-07-24 프레이밍 수정 재QA

### 판정

- **프레이밍 결함 수정 통과**
- **설정 동기화 1건 수정 필요 / 현재 사용자 제시 보류**

### 통과 항목

- 16개 PNG 실제 알파 bbox 최소 여백: `4px`
  - W 정지·보행 키 좌측 여백: 각각 `4px`
  - E 정지·보행 키 우측 여백: 각각 `4px`
- 16개 전체 캔버스 경계 접촉: `0건`
- 전부 `128×128 RGBA`, 알파 `[0,255]`
- 전체 공용 불투명색 `25색`, 최대 32색 계약과 무디더 유지
- 정지/보행 키는 전 방향에서 서로 다르며 대표 포즈 관계 유지
- 비교표 `2048×512`, 턴어라운드 `2048×640` 갱신 확인
- 시각 대조상 프레이밍 축소·평행 이동 외 체형·팔레트·방향·포즈의 비의도 변경은 확인되지 않았다. 외형 취향과 최종 채택은 사용자 판단으로 남긴다.
- 저장된 `.blend`와 스크립트·설정 JSON의 카메라 위치, 직교 투영, `ortho_scale=6.8`, `128×128`, 투명 배경은 일치한다.

### 남은 설정 동기화 결함

- 저장된 `.blend`의 `scene.render.fps`는 `24`다.
- `render-settings.json`은 타임라인 FPS를 `8`로 기록한다.
- 재현 스크립트는 JSON에 `fps: 8`을 쓰지만 `scene.render.fps = 8`은 설정하지 않는다.
- `.blend`·재현 스크립트·설정 자료 동기화 조건을 통과하려면 Blender 씬 FPS를 8로 명시하고 `.blend`를 다시 저장한 뒤 해당 값만 재확인해야 한다.

## 2026-07-24 FPS 수정 최종 재QA

### 판정

- **통과**
- **프로젝트 총괄 검토 후 사용자 제시 가능**
- 현재 결과는 최종형에 가까운 후보이며 사용자 시각 채택 전 최종 제품 아트로 확정하지 않는다.

### 최종 대조

- 저장된 `.blend`
  - `scene.render.fps = 8`, `fps_base = 1.0`
  - 프레임 범위 `1~8`
  - 기존 통과 설정 `128×128`, 투명 배경, 직교 스케일 `6.8` 유지
- 재현 스크립트
  - `scene.frame_start, scene.frame_end = 1, 8`
  - `scene.render.fps = 8` 명시
  - Python AST 구문 분석 통과
- `render-settings.json`
  - FPS `8`, 프레임 범위 `[1,8]`, 정지 프레임 `1`, 보행 키 프레임 `4`
- 기존 프레이밍·규격 유지 확인
  - 16개, 전부 `128×128 RGBA`
  - 최소 bbox 여백 `4px`, 경계 접촉 `0건`
  - 알파 `[0,255]`, 공용 불투명색 `25색`

## 2026-07-24 v2-natural 독립 QA

### 판정

- **통과**
- **프로젝트 총괄 검토 후 v1/v2 비교 자료를 사용자에게 제시 가능**
- v2-natural의 최종 외형 채택과 v1 대비 선호 판단은 사용자 시각 결정 사항이다.

### 산출물·기술 규격

- 별도 원본·재현
  - `artifacts/v2-natural/source/rat-final-appearance-sample-v2-natural.blend`
  - `artifacts/v2-natural/source/create_rat_final_appearance_sample_v2_natural.py`
  - 재현 스크립트 Python AST 구문 분석 통과
- 정지 8방향 + 보행 키 8방향 = 16개
  - 전부 `128×128 RGBA`
  - 알파 `[0,255]`
  - 공용 불투명색 25색, 최대 32색 이하
  - 무디더
  - 실제 최소 bbox 여백 4px, 캔버스 경계 접촉 0건
  - 전 방향에서 정지/보행 키의 실제 픽셀 차이 확인
- 저장된 `.blend`
  - FPS 8, 프레임 1~8
  - `128×128`, 투명 배경, ORTHO `6.8`
  - v2 루트 위치 `(0,0,0)`, 회전 `(0,0,0)`, 스케일 `(1,1,1)`, root action 없음
- `frame-map.csv`, `render-settings.json`, `palette-statistics.json`은 실제 방향·프레임·규격·팔레트 결과와 일치한다.
- 부가 이미지
  - v2 contact sheet `2048×512`
  - v2 turnaround `2048×640`
  - v1/v2 comparison `2048×512`
  - contact sheet와 comparison은 원본 PNG 재조합 결과와 픽셀 단위로 일치한다.

### 방향·포즈·접지

- 방향 순서 `S, SW, W, NW, N, NE, E, SE`와 정지 1프레임·보행 키 4프레임이 프레임 맵에 일치한다.
- v1과 같은 카메라·방향 yaw·피벗 언어를 유지한다.
- 보행 키에서 발 배치가 실제로 바뀌며 큰 상하 점프 없이 낮은 잰걸음 관계가 유지된다.
- v1/v2 동일 방향 bbox 하단 차이가 0~1px 범위이고, 시각 대조에서 발이 지면 아래로 파고들거나 몸이 뜨는 현상은 확인되지 않았다.

### 자연화 요구 반영

- 몸통·목·머리 연결이 v1보다 둥글고 연속적인 덩어리로 읽힌다.
- 삼각형 단위의 faceted 명암 분절과 얼굴의 넓은 크림 쐐기가 줄었다.
- 측면·대각선 몸통은 약 3~5개의 큰 유기적 명암 군집으로 정리되어 작은 삼각 조각보다 큰 부피가 우선한다.
- 주둥이는 더 부드러운 쐐기형이며 발은 수직 부피가 줄어든 납작한 타원형으로 읽힌다.
- 큰 귀, 작은 반사 눈, 코 끝, 가는 긴 꼬리, 8방향 진행 판독성은 유지된다.
- 위 항목은 제작 요구 충족 여부만 판정한 것이며 v1/v2 중 어느 외형을 채택할지는 사용자 판단으로 남긴다.

### v1 동결·프로젝트 보존

- v2 설정에 기록된 v1 `.blend`, 재현 스크립트, contact sheet SHA-256은 현재 파일과 모두 일치한다.
- v1 contact sheet는 현재 v1 정지 8장·보행 키 8장의 최근접 재조합과 픽셀 단위로 동일하며, SHA-256 `e91edb36...1cf8fc5`도 이전 QA 값과 같다.
- v1 turnaround SHA-256 `6cec40af...cad5d3`도 이전 QA 값과 같다. 이에 따라 v1 원본·스크립트·16 PNG·contact·turnaround 핵심 20개 보존을 확인했다.
- UnityProject의 기존 수정 2건은 이전 QA와 같은 상태다. `Builds/`와 `_workspace/completed/`에 이번 v2 작업으로 생긴 tracked diff·삭제는 없다.
- 이 QA는 Unity 실행·아트 수정·폴더 이동·커밋을 수행하지 않았다.

## 2026-07-26 A2 Blender revision-6 독립 QA

### 최종 판정

- **수정 필요**
- 기술 이미지 규격과 A2 시각 최소 게이트는 통과했으나, `idle` 8장이 실제 중립 정지 포즈가 아니므로 사용자 제시를 보류한다.
- r1~r5는 후보 판정에 사용하지 않았고, 후속 정리에서 중간 바이너리를 삭제·커밋 제외하며 반려 사유만 문서에 보존했다.

### 기술 규격 — 통과

- 최종 후보: `artifacts/a2-blender-revision-6/`
- 원본·재현
  - `source/rat-final-appearance-a2-r6.blend`: 존재, 단일 루트에서 모든 방향 생성
  - `source/create_rat_final_appearance_a2_r6.py`: 존재, Python AST 구문 분석 통과
- 실제 PNG 재계산
  - 정지 표기 8개 + 보행 키 8개 = 16개
  - 전부 `128×128 RGBA`
  - 전체 알파 `[0,255]`
  - 16장 공용 불투명색 25색, 최대 32색 이하
  - 무디더
  - 최소 bbox 여백 4px, 캔버스 경계 접촉 0건
  - 모든 방향에서 정지 표기/보행 키 이미지의 실제 픽셀 차이 확인
- 방향·자료
  - 방향 순서 `S, SW, W, NW, N, NE, E, SE`
  - contact `2048×512`, turnaround `2048×640`
  - contact는 현재 16개 PNG의 2배 최근접 재조합과 픽셀 단위로 일치
  - `frame-map.csv`, `render-settings.json`, `palette-statistics.json`의 수량·경계·팔레트 기록은 실제 파일과 일치
- 저장 `.blend`
  - Blender 소스 렌더 `512×512`, 투명 배경, FPS 8, 프레임 1~8
  - 직교 카메라, `ortho_scale=5.3`
  - 루트 위치 `(0,0,0)`, 회전 `(0,0,0)`, 스케일 `(1,1,1)`, root action 없음
  - 같은 루트를 방향별 yaw로 돌리고 고정 카메라·전체 캔버스를 사용해 방향 간 공통 피벗이 유지된다.
- 승인된 PPU64·Custom Pivot `(0.5,0.25)`은 Unity 반입 전 외부 계약이며, 이번 샘플에는 Unity Import 설정을 만들거나 적용하지 않았다.

### 차단 결함 — 정지 포즈 계약 불일치

- `idle` 출력은 프레임 1을 사용하고 `frame-map.csv`에는 `neutral_idle`로 기록되어 있다.
- 그러나 저장 원본과 재현 스크립트에서 프레임 1은 이미 대각선 보행 위상이다.
  - 접지: `Paw_FL minZ≈0.010`, `Paw_RR minZ≈0.005`
  - 들린 발: `Paw_FR minZ≈0.095`, `Paw_RL minZ≈0.090`
  - FR/RL은 기본 위치에서 전후 이동과 상승이 적용되어 있다.
- 따라서 현재 첫 행 8장은 “정지 8방향”이 아니라 보행 위상 A이며, 둘째 행은 보행 위상 B다. 정지·대표 보행 키 두 종류를 제공한다는 산출물 계약을 충족하지 못한다.
- 수정 조건:
  1. 실제 네 발 접지의 중립 정지 포즈로 idle 8장을 재생성한다.
  2. contact·turnaround·frame map·settings·palette stats를 다시 동기화한다.
  3. 보행 키 f04와의 차이, 접지, 16장 규격·경계·팔레트를 재QA한다.

### A2 시각 최소 게이트 — 통과, 위험 기록

- A2의 낮고 긴 갈색쥐 방향은 측면·대각선에서 판독된다.
- 길어진 주둥이와 긴 가는 꼬리 때문에 짧은 얼굴·짧은 꼬리의 햄스터 실루엣으로 읽히지 않는다.
- 등·옆구리·배는 작은 삼각 조각보다 큰 갈색 명암 덩어리로 정리되어 있다.
- 귀, 작은 반사 눈, 어두운 코, 주둥이, 발·발가락, 수염, 꼬리와 8방향 진행 방향은 128px contact와 확대 turnaround에서 판독 가능하다.
- 남은 시각 위험:
  - 측면 몸통의 길쭉한 캡슐형 외곽과 띠 모양 명암이 A2의 자연스러운 털·몸통 연결보다 패널처럼 보일 수 있다.
  - 근거리 귀가 A2보다 크게 지배하고 얼굴의 어두운 귀/눈 대비가 강해 일부 방향에서 눈이 묻힐 수 있다.
- 위 위험은 최소 후보 제시를 막는 수준으로 판정하지 않으며, 최종 외형 수용 여부는 사용자 시각 판단으로 분리한다.

### 보존·금지 범위

- 당시 기존 v1·v2-natural·v5b, A/B/C/A2와 r1~r5를 확인했다. 후속 정리에서 r1~r5 중간 바이너리는 삭제·커밋 제외하고 반려 사유만 문서에 보존했다.
- v1 `.blend` SHA-256 `35f6e93f...e1190a`는 이전 독립 QA 기준과 일치한다.
- v2 및 A/B/C/A2 파일은 r6보다 앞선 수정 시각을 유지하고 별도 r6 경로에서 생성되었다. 기존 경로 삭제는 확인되지 않았다.
- UnityProject에는 이전 QA와 같은 기존 수정 2건만 보이며, 이번 작업 관련 Unity·Builds·completed tracked 삭제/변경은 확인되지 않았다.
- r6에는 전체 64프레임, runtime atlas·스프라이트 시트, Unity Import·통합 산출물이 없다.
- 이 QA는 직접 아트 수정, Unity 실행·변경, 폴더 이동, 커밋을 수행하지 않았다.

## 2026-07-26 r6 neutral idle 수정 재QA

### 판정

- **통과**
- 이전 차단이었던 정지 포즈 계약 불일치가 해소됐다.
- 프로젝트 총괄 검토 후 사용자에게 A2 Blender 최소 후보로 제시 가능하다. 최종 외형 수용은 사용자 시각 판단이다.

### neutral idle·보행 키 독립 대조

- 저장된 `.blend`와 재현 스크립트의 타임라인: frame 0~8, FPS 8
- frame 0 neutral idle
  - `Paw_FL`, `Paw_FR` world bbox min Z `≈0.010000005`
  - `Paw_RL`, `Paw_RR` world bbox min Z `≈0.009999998`
  - 네 발 모두 전후 stride offset 없이 공통 지면에 접지
- frame 4 walk key
  - 접지: `Paw_FR`, `Paw_RL` min Z `≈0.01`
  - 들린 발: `Paw_FL`, `Paw_RR` min Z `≈0.070104`
  - 대각선 다리쌍 보행 위상이 neutral idle과 명확히 구분됨
- `frame-map.csv`는 idle frame 0을 `neutral_four_paw_ground_contact`, walk frame 4를 `diagonal_FL_RR_lift`로 기록하며 실제 원본과 일치한다.
- idle/walk 실제 픽셀 차이: 전 방향 274~610픽셀

### 기술 규격 유지

- 16개, 전부 `128×128 RGBA`
- 알파 `[0,255]`, 공용 불투명색 25색, 무디더
- 최소 bbox 여백 4px, 캔버스 경계 접촉 0건
- 방향 순서 `S, SW, W, NW, N, NE, E, SE`
- contact는 현재 16개 PNG 최근접 재조합과 픽셀 단위로 일치
- contact·turnaround·frame map·settings·palette stats·`.blend`·재현 스크립트가 frame 0 idle 기준으로 동기화됨

### 시각·보존 범위

- A2 시각 최소 게이트의 낮고 긴 갈색쥐, 긴 주둥이·꼬리, 큰 명암 덩어리, 귀·눈·코·발·수염·방향 가독성은 유지된다.
- 이전에 기록한 캡슐/패널형 몸통 띠와 큰 귀·어두운 얼굴 대비 위험은 사용자 시각 판단 항목으로 유지한다.
- v1·v2-natural·A/B/C/A2와 r6 최종 산출물은 유지한다. r1~r5 중간 바이너리는 후속 정리에서 삭제·커밋 제외했고 반려 사유만 문서에 보존한다.
- UnityProject의 기존 수정 2건은 유지되고, Builds·completed의 이번 수정 관련 tracked 변경·삭제는 없다.
- 전체 64프레임, runtime atlas·스프라이트 시트, Unity Import·통합은 없다.
- 이 QA는 직접 아트·Unity·폴더 이동·커밋을 수행하지 않았다.

## 완료 판단

통과 — r6 neutral idle·보행 키·기술 규격·A2 시각 최소 게이트 확인, 총괄 검토 후 사용자 제시 가능

## 2026-07-27 반려 중간 바이너리 정리 확인

- 부재 확인: `a2-blender/`(r1), `a2-blender-revision-2/`~`revision-5/`, r6 `previews/`, r6 `source/*-preview.blend`.
- 유지 확인: AI concepts, v1·v2-natural, A2 참고안, r6 최종 재현 스크립트·`.blend`, idle 8, walk-key 8, contact, turnaround, settings, stats, frame-map.
- 정리 보고 용량: `1.38 MiB`.
- 판정 영향: r6 neutral idle 재QA와 총괄 판정은 유지한다. 사용자 최종 수용 대기이며 완료 처리·보관하지 않는다.
- 범위 유지: 전체 64프레임, runtime atlas/스프라이트 시트, Unity 반입은 미승인이다.
