# 검증 기록

## 작업 ID

2026-07-01-quarter-left-rear-camera

## 검증 대상

쥐 숙주 프로토타입의 쿼터뷰 카메라를 바로 뒤편에서 약간 왼쪽 후방으로 조정한 변경.

## 검증 담당

Codex 메인 에이전트, QA/검증 에이전트 `Hegel`

## 검증 에이전트 수행 이력

- 검증 에이전트: QA/검증 에이전트 `Hegel`
- 검증 요청자: Codex 메인 에이전트
- 검증한 산출물: 코드/테스트 변경, 씬 빌더/씬 변경, Unity 테스트/Play 검증 결과
- `agent-activity.md` 반영 여부: 완료

## 입력 자료

- 사용자 피드백
- `PrototypeCameraController.cs`
- `RatHostPrototypeCoreTests.cs`
- `RatHostPrototypeSceneBuilder.cs`
- `RatHostPrototype.unity`
- 구현 에이전트 결과

## 원래 증상 또는 완료 주장

- 원래 증상: 쿼터뷰가 바로 뒤편에서 보는 카메라라 화면 감각이 덜 자연스럽다.
- 완료 주장: 쿼터뷰가 `(-2.8, 7.4, -6.4)`의 왼쪽 후방 고정 시점으로 조정되었고, 후방 성분이 더 커서 측면뷰가 되지 않는다.

## 독립 검증 여부

- 구현 주체와 검증 주체 분리 여부: 완료
- 구현 주체가 실행한 검증과 별도로 확인한 항목: QA/검증 에이전트가 구현 diff, 작업 패킷, 메인 통합 검증 기록, 역할 분리 기록을 확인

## 실행한 검증

```text
명령 또는 확인 방법:
게임플레이 구현 에이전트 RED 확인

결과:
테스트만 변경한 상태에서 컨트롤러 기본값 테스트와 현재 씬 오프셋 테스트가 `x=0` 때문에 실패.

해석:
새 테스트가 기존 바로 뒤편 카메라 값을 실제로 잡아낸다.
```

```text
명령 또는 확인 방법:
Unity RunCommand로 `RatHostPrototypeCoreTests` 18개 명시 호출

결과:
18/18 통과.

해석:
카메라 전환 순서, 왼쪽 후방 쿼터뷰, 탑뷰, 씬 오프셋, 기존 핵심 루프 테스트가 통과했다.
```

```text
명령 또는 확인 방법:
Unity Play 모드에서 카메라 전환 검증

결과:
통과.
- 쿼터뷰 오프셋: `(-2.80, 7.40, -6.40)`
- 쿼터뷰 수평 forward: `(0.40, 0.00, 0.92)`
- 탑뷰 오프셋: `(0.00, 9.50, 0.00)`

해석:
쿼터뷰는 왼쪽 후방이며, 수평 forward의 z 성분이 우세해서 측면뷰가 아니다. 탑뷰와 3인칭 복귀도 유지된다.
```

```text
명령 또는 확인 방법:
Unity Console Error 확인

결과:
Error 0건.

해석:
검증 후 Unity 콘솔 오류 없음.
```

```text
명령 또는 확인 방법:
git diff --check

결과:
오류 없음. CRLF 변환 경고만 있음.

해석:
패치 공백 오류 없음.
```

```text
명령 또는 확인 방법:
Unity MCP `Unity_RunCommand` fresh quarter camera verification

결과:
통과.
- 직렬화 쿼터뷰 오프셋: `(-2.80, 7.40, -6.40)`
- 런타임 쿼터뷰 오프셋: `(-2.80, 7.40, -6.40)`
- 쿼터뷰 수평 forward: `(0.40, 0.00, 0.92)`
- 쥐 viewport: `(0.50, 0.50, 9.92)`
- 탑뷰 오프셋: `(0.00, 9.50, 0.00)`

해석:
현재 씬 기준으로 왼쪽 후방 쿼터뷰, 쥐 프레이밍, 탑뷰 전환, 3인칭 복귀 조건이 유지된다.
```

```text
명령 또는 확인 방법:
Unity MCP `Unity_GetConsoleLogs` Error 조회

결과:
Error 0건.

해석:
최종 검증 시점의 Unity 콘솔 오류 없음.
```

## 검증하지 못한 항목

- 실제 사용자의 체감 플레이 확인은 아직 수행하지 않음

## 실패 또는 경고

- `.codex/config.toml`은 기존 사용자/환경 변경이며 이번 작업과 커밋 대상에서 제외해야 함
- 카메라 기준 입력 축이 약간 회전하므로 최종 감각은 사용자 플레이 확인이 필요함

## QA/검증 에이전트 판정

```text
판정:
조건부 승인

확인한 항목:
- 코드 기본값, 씬 빌더, 현재 씬의 `quarterViewOffset`가 모두 `(-2.8, 7.4, -6.4)`로 일치한다.
- `abs(x)=2.8`, `abs(z)=6.4`라 왼쪽 후방이면서 후방 성분이 우세하다.
- 테스트가 왼쪽 후방 여부, 후방 성분 우세, 수평 forward의 z 우세 조건을 직접 검증한다.
- 특정 외부 게임 카메라 복제 금지 범위를 벗어나지 않았다.
- 구현 담당은 게임플레이 구현 에이전트와 Unity 씬/통합 구현 에이전트로 분리되어 있다.
- 메인 통합 검증 기록상 `RatHostPrototypeCoreTests` 18/18 통과, Play 검증 통과, Unity Console Error 0건, `git diff --check` 오류 없음.
- `.codex/config.toml`은 이번 판정과 커밋 대상에서 제외한다.

발견한 문제:
- 구현 자체의 반려 사유는 없다.
- QA 판정 문서 반영과 프로젝트 총괄 관리자 판정이 남은 차단 조건이다.

남은 위험:
- QA가 Play 검증을 재실행한 것은 아니고 메인 통합 검증 기록과 diff 기준으로 판정했다.
- 왼쪽 후방 오프셋의 최종 체감은 사용자 플레이 확인이 필요하다.
```

## 게이트 판정

- QA/검증 게이트 통과 여부: 조건부 승인
- `agent-activity.md`에 QA 판정 반영 여부: 완료
- 프로젝트 총괄 관리자 판정: 내부 승인 가능

## 완료 판단

- 완료 가능

## 완료 판단 근거

- 구현 에이전트 산출물, 메인 통합 검증, QA/검증 에이전트 조건부 승인, 프로젝트 총괄 관리자 내부 승인 가능 판정이 있다.
