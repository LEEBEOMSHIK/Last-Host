# 작업 로그

## 작업 ID

`2026-06-30-docs-structure-organize`

## 로그

### 2026-06-30

- 수행 내용: 사용자 요청을 문서 구조 정리 작업으로 분류하고 작업 패킷을 만들었다.
- 확인한 자료: `AGENTS.md`, `docs/`, `.agents/`, `.codex/skills/last-host-design-keeper`
- 판단: 문서 이동 자체는 승인됐지만, Unity 구현과 프로토타입 범위 변경은 하지 않는다.
- 다음 작업: 문서 이동과 참조 경로 갱신을 진행한다.

- 수행 내용: `docs/` 문서를 성격별 하위 폴더로 이동하고 `docs/README.md` 색인을 추가했다.
- 갱신 범위: `AGENTS.md`, `docs/`, `.agents/`, `.codex/skills/`, 현재 `_workspace/active/` 작업 문서
- 판단: 과거 완료 이력은 당시 기록이므로 보존하고, 현재 진행/운영 문서만 새 경로로 맞췄다.

- 수행 내용: `포유류 적응` 관련 `1차 효과` 표현을 `1차 프로토타입 임시 효과`로 정리했다.
- 판단: 1차 빌드에 두 번째 숙주가 없기 때문에 정식 숙주 전이가 아니라 하수도 맵 안에서만 적용되는 임시 체감 효과로 설명한다.

## 결정 기록

- `docs/agents/`: 에이전트/스킬 운영 문서
- `docs/design/`: 게임 기획 요약
- `docs/prototype/`: 쥐 숙주 프로토타입 범위와 승인 패킷
- `docs/project/`: 프로젝트 준비 상태
- `docs/unity/`: Unity 기준 상태와 MCP 설정

## 열린 질문

- 없음. 다만 `포유류 적응`의 실제 임시 효과 선택지는 기존 승인 패킷에서 사용자 승인 항목으로 유지한다.

## 위험과 주의점

- 에이전트, 스킬, 문서 안에 남은 옛 `docs/<파일명>` 참조가 있으면 다음 작업에서 혼선이 생긴다.
- `_workspace/completed/`의 과거 이력 문서는 당시 경로 기록이므로 불필요하게 고치지 않는다.
