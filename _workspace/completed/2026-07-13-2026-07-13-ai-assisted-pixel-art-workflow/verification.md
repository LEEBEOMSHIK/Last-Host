# 검증 기록

## 작업 ID

`2026-07-13-ai-assisted-pixel-art-workflow`

## 검증 대상

- `docs/design/visual/pixel-lowpoly-3d-production-guide.md`의 §10 AI 보조 래스터 제작 규칙
- `docs/prototype/plans/rat-host-ai-assisted-art-workflow.md`의 자산 묶음별 7단계 계획
- 관련 README·참조 색인·공유 상태판과 작업 기록

## 검증 담당

QA/검증 에이전트

## 검증 에이전트 수행 이력

- 검증 에이전트: QA/검증 에이전트
- 검증 요청자: 프로젝트 조정 에이전트
- 검증한 산출물: AI 보조 래스터 규칙, 쥐 숙주 AI 보조 아트 작업 순서, 문서 색인, 상태판, 작업 패킷
- `agent-activity.md` 반영 여부: 반영함

## 입력 자료

- `AGENTS.md`
- `docs/design/game-design-summary.md`
- `docs/project/project-prep.md`
- `.codex/skills/pixel-lowpoly-style-keeper/SKILL.md` 및 스타일 규칙
- `docs/agents/loop-engineering-gates.md`
- 작업 패킷의 `task.md`, `handoff.md`, `agent-activity.md`, `work-log.md`

## 원래 증상 또는 완료 주장

ChatGPT 등 이미지 생성 도구를 도트풍 3D의 래스터 초안에만 보조적으로 쓰고, 실제 3D·Unity 작업과 사용자 승인·기록·검증을 분리한 규칙과 실행 순서를 문서화했다.

## 독립 검증 여부

- 구현 주체와 검증 주체 분리 여부: 분리됨. 문서/릴리즈 에이전트의 문서 반영 뒤 QA/검증 에이전트가 별도로 대조했다.
- 구현 주체가 실행한 검증과 별도로 확인한 항목: 허용/비대체 경계, 묶음별 사용자 승인, 프롬프트·출처·권리·개인정보·파일 위치 기록, 7단계와 자산 묶음, 역할·수용 기준·위험, README·참조 색인·상태판·Git 변경 범위.

## 실행한 검증

```text
명령 또는 확인 방법:
문서 본문과 작업 패킷을 읽어 AI 보조 래스터의 허용 용도, 메시·UV·리깅·Unity 비대체 경계, 묶음별 사용자 승인과 기록·권리·개인정보 규칙을 대조했다.
결과:
§10에 콘셉트·픽셀 텍스처·타일·HUD 아이콘 초안만 허용하고, 최종 메시·UV·리깅·콜라이더·Import·URP·플레이 검증은 대체할 수 없다고 명시했다. 자산 묶음별 실제 생성 전 사용자 승인, 프롬프트·도구·날짜·참조 출처·선택 변형·파일 위치 기록, 제3자 권리·개인정보 제한도 확인했다.
해석:
AI 출력이 순수 2D 게임 전환이나 승인 없는 3D·Unity 변경으로 이어지지 않도록 경계가 충분하다.

명령 또는 확인 방법:
새 작업 계획의 표를 정적 검사해 1~7단계와 쥐 숙주·하수도·내부 면역 미니게임 자산 묶음, 역할·수용 기준·위험 대응을 확인했다.
결과:
자산 묶음 승인 → 스타일 브리프·목록 → 승인 범위 생성 → 사람 선별·정리 → 저폴리 메시·UV·머티리얼 → Unity 통합 → 시각·플레이 QA 순서가 모두 존재한다. 단계별 진입 기준, 담당, 기록, 다음 단계 기준과 사용자 결정 시점이 있다.
해석:
실제 생성·3D·Unity 작업을 자동으로 승인하지 않고 각 단계의 별도 사용자 승인과 QA를 요구한다.

명령 또는 확인 방법:
README·`agent-reference-map.md`·상태판·Git 상태를 대조하고, `git diff --check`와 새 문서의 `git diff --no-index --check`를 실행했다.
결과:
새 계획은 `prototype/plans/`에, 비주얼 규칙은 `design/visual/`에 색인됐다. 상태판에는 작업의 active 경로, 다음 작업 후보 없음, 보류 1건(사용자 수동 플레이)과 문서 전용 미커밋 범위가 일치한다. UnityProject, 코드, 씬, ProjectSettings, 패키지 변경은 Git 상태에서 없다. diff-check는 공백 오류 없이 통과했고 CRLF 변환 예고와 사용자 범위 밖 `.codex/config.toml` 변경만 관찰됐다.
해석:
문서 배치·참조·상태판·변경 범위가 작업 배정서의 금지 범위와 일치한다.
```

## 검증하지 못한 항목

- 실제 이미지 생성 품질, 권리 상태, 도구 이용 약관은 실제 자산 묶음 승인과 생성 시점에 별도 확인해야 한다.
- 저폴리 메시·UV·리깅·Unity Import·URP·플레이 화면은 이번에 생성·변경하지 않았으므로 검증하지 않았다.

## 실패 또는 경고

- `git diff` 계열은 Git 전역 ignore 파일 접근 권한 경고와 CRLF 변환 예고를 출력했지만, 공백 오류나 Unity 경로 변경은 없었다.
- `_workspace/active/CURRENT.md`를 재대조한 결과, 현재 작업 ID와 `QA 조건부 통과, 프로젝트 총괄 관리자 검토 대기` 상태를 정확히 가리킨다. 기존 포인터 불일치는 해소됐다.

## 게이트 판정

- QA/검증 게이트 통과 여부: 통과. 문서 규칙·범위·상태판·Git 변경 범위와 현재 세션 포인터를 재대조했다.
- `agent-activity.md`에 QA 판정 반영 여부: 반영함
- 총괄 관리자 검토로 넘길 수 있는지: 가능.

## 완료 판단

완료 가능

## 완료 판단 근거

문서 산출물은 QA 기준을 충족한다. 실제 이미지·Unity 변경은 없고, 상태판은 진행 중 active 경로·후보 0건·사용자 수동 플레이 보류 1건·Git 범위를 정확히 표시한다. 최종 완료·보관과 커밋 여부는 프로젝트 총괄 관리자 판정 뒤에만 진행한다.
