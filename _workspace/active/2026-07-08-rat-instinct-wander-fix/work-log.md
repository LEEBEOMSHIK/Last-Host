# 작업 로그: 쥐 숙주 본능 이동 왕복 문제 수정

## 2026-07-08

- 사용자 제보: 쥐가 랜덤으로 움직이지 않고 위아래로 왕복하며, 좌우 입력 시 대각선으로 움직인다.
- 원인 조사: 본능 방향이 `Vector3.forward`로 고정되고, 경계에서 축 반전만 수행해 왕복한다.
- 판단: 낮은 조종력에서 대각 이동은 의도된 모델이지만, 본능 이동의 고정 왕복은 버그/미완성 구현으로 본다.
- 주의: 직전 비활성 `CharacterController.Move` 수정이 같은 파일에 미커밋 상태로 남아 있으므로 함께 보존한다.
- RED 테스트: 본능 주기 전환이 forward를 꺾는지, 경계에서 위아래 반전 대신 벽을 타는 방향으로 회피하는지 테스트를 추가했다.
- 구현: `RatHostInstinctWanderModel`을 추가하고, RatHostController가 초기 방향 랜덤화/주기적 방향 전환/경계 회피를 사용하게 연결했다.
- 검증: 런타임/테스트 어셈블리 수동 컴파일 통과, `git diff --check` 통과, Unity MCP Play 2초 후 Console Error/Warning 0건 확인.
